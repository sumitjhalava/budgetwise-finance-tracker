# BudgetWise Finance Tracker - Architecture

## 🏗️ System Architecture

### High-Level Architecture
```
┌─────────────────┐    HTTP/REST    ┌─────────────────┐    JPA/Hibernate    ┌─────────────────┐
│   React.js      │ ──────────────► │   Spring Boot   │ ──────────────────► │     MySQL       │
│   Frontend      │ ◄────────────── │    Backend      │ ◄────────────────── │    Database     │
│                 │    JSON/JWT     │                 │      SQL Queries    │                 │
└─────────────────┘                 └─────────────────┘                     └─────────────────┘
```

## 🔧 Backend Architecture (Spring Boot)

### Layer Structure
```
┌─────────────────────────────────────────────────────────────┐
│                    Presentation Layer                       │
│  ┌─────────────────┐  ┌─────────────────┐                 │
│  │ AuthController  │  │TransactionCtrl  │                 │
│  └─────────────────┘  └─────────────────┘                 │
└─────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────┐
│                     Security Layer                          │
│  ┌─────────────────┐  ┌─────────────────┐                 │
│  │   JWT Filter    │  │  Security Config│                 │
│  └─────────────────┘  └─────────────────┘                 │
└─────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────┐
│                  Data Access Layer                          │
│  ┌─────────────────┐  ┌─────────────────┐                 │
│  │  UserRepository │  │TransactionRepo  │                 │
│  └─────────────────┘  └─────────────────┘                 │
└─────────────────────────────────────────────────────────────┘
```

### Component Details

#### Controllers
- **AuthController**: Handles user registration and login
- **TransactionController**: Manages CRUD operations for transactions

#### Security Components
- **JwtUtil**: JWT token generation and validation
- **JwtFilter**: Intercepts requests and validates JWT tokens
- **SecurityConfig**: Spring Security configuration

#### Data Models
- **User**: User entity with authentication details
- **Transaction**: Financial transaction entity

## 🎨 Frontend Architecture (React.js)

### Component Structure
```
App.js
├── Router
│   ├── Login.js
│   ├── Signup.js
│   └── Dashboard.js
│       └── Layout.js
└── ProtectedRoute.js
```

### Service Layer
```
services/
├── api.js (Axios configuration)
utils/
└── auth.js (Authentication utilities)
```

## 🔐 Security Architecture

### Authentication Flow
```
1. User Login Request → 2. Backend validates → 3. JWT generated → 
4. Token to frontend → 5. Token in sessionStorage → 6. Token in API requests
```

### Data Isolation
- User ID embedded in JWT token
- Database queries filtered by user ID
- Authorization checks in controllers

## 💾 Database Design

### Entity Relationship
```
┌─────────────────┐         ┌─────────────────┐
│      User       │         │   Transaction   │
├─────────────────┤         ├─────────────────┤
│ id (PK)         │ 1    ∞  │ id (PK)         │
│ name            │ ────────│ user_id (FK)    │
│ email (UNIQUE)  │         │ description     │
│ password        │         │ amount          │
└─────────────────┘         │ type            │
                            │ category        │
                            │ date            │
                            └─────────────────┘
```

## 🚀 Deployment

### Development
```
Localhost:3000 (React) → Localhost:8081 (Spring Boot) → Localhost:3306 (MySQL)
```

### Production Recommendations
- Use HTTPS/TLS
- Environment variables for secrets
- Database connection pooling
- Load balancer for scaling